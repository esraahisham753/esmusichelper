% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_duration.R
\name{get_duration}
\alias{get_duration}
\title{Get Duration in Note Values}
\usage{
get_duration(ticks, ticks_per_beat)
}
\arguments{
\item{ticks}{Numeric. The number of ticks representing the duration.}

\item{ticks_per_beat}{Numeric. The number of ticks in a single beat.}
}
\value{
Character. The note value as a string, such as \code{"whole"}, \code{"half"}, \code{"quarter"},
\code{"eighth"}, \code{"sixteenth"}, or \code{"unknown"} if the note value does not match a standard duration.
}
\description{
Converts a duration given in ticks to a standard musical note value
(e.g., whole, half, quarter, eighth, sixteenth).
}
\examples{
get_duration(480, 120) # Returns "whole" (4 beats)
get_duration(240, 120) # Returns "half" (2 beats)
get_duration(120, 120) # Returns "quarter" (1 beat)
get_duration(60, 120)  # Returns "eighth" (0.5 beat)
get_duration(30, 120)  # Returns "sixteenth" (0.25 beat)
}
